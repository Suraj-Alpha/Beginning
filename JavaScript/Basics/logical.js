var isregistered = true;
var isOnline = true;
var isUploading = true;
var isDowloading = false;

if (isregistered && isOnline)
    {
        console.log("Welcome")
    }
if (isUploading||isDowloading)
{
    console.log("thanks for work")
}